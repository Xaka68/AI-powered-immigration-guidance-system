# Backend runtime deps for App Runner (source-based build).
# Lean "api" set: no torch / chromadb / sentence-transformers yet, because
# retrieval (Track B) isn't wired — the app boots and degrades gracefully.
# When Track B lands, add: chromadb>=0.5  sentence-transformers>=3.0
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pydantic>=2.6
python-dotenv>=1.0
jsonschema>=4.21
openai>=1.30
